# dependencies / build
node_modules
.next
out
*.tsbuildinfo
next-env.d.ts

# python recognizer
recognizer/.venv
**/__pycache__/
*.pyc
.python-version

# secrets / env
.env
.env*.local

# runtime data (play history)
.data/

# hook + tool logs (may contain secrets/tool output) — never commit
logs/
**/logs/

# os
.DS_Store
